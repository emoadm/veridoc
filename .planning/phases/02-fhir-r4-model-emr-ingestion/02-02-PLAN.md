---
phase: 02-fhir-r4-model-emr-ingestion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy/helm/veridoc/templates/mongodb.yaml
  - deploy/helm/veridoc/templates/minio.yaml
  - deploy/helm/veridoc/values.yaml
  - deploy/helm/veridoc/templates/secrets.yaml
autonomous: true
requirements: [EMR-01]
user_setup: []

must_haves:
  truths:
    - "MongoDB renders as a single-node Deployment with credentials from a K8s Secret (never inlined)"
    - "MinIO renders as a single-node Deployment exposing API (9000) + console (9001) with credentials from a Secret"
    - "helm template renders the chart with mongodb + minio enabled and no plaintext secrets in the manifests"
  artifacts:
    - path: "deploy/helm/veridoc/templates/mongodb.yaml"
      provides: "MongoDB Deployment + Service (D-02 clinical-document store)"
      contains: "MONGO_INITDB_ROOT_USERNAME"
    - path: "deploy/helm/veridoc/templates/minio.yaml"
      provides: "MinIO Deployment + Service (D-10 blob store)"
      contains: "MINIO_ROOT_USER"
    - path: "deploy/helm/veridoc/values.yaml"
      provides: "mongodb + minio + ingestionService value sections + secrets refs"
      contains: "veridoc-docs"
  key_links:
    - from: "deploy/helm/veridoc/templates/mongodb.yaml"
      to: "deploy/helm/veridoc/templates/secrets.yaml"
      via: "secretKeyRef on mongodb credentials"
      pattern: "secretKeyRef"
    - from: "deploy/helm/veridoc/templates/minio.yaml"
      to: "deploy/helm/veridoc/templates/secrets.yaml"
      via: "secretKeyRef on minio credentials"
      pattern: "secretKeyRef"
---

<objective>
Stand up the deferred datastores (D-10): add MongoDB (D-02 clinical-document store) and a
MinIO/S3-compatible blob store (D-10 retained originals) to the Helm chart the same
provider-portable, secret-referenced way Postgres + Redis landed in Phase 1 — but WITHOUT
the ingestion-service Deployment (that lands in 02-07 once the image exists).

Purpose: Mongo + blob infra are dependencies of the service-wiring wave; landing them now
(parallel with package/fixture work) lets the final wave focus on the service + CI smoke.
Output: mongodb.yaml + minio.yaml templates, values.yaml sections, and secrets refs, all
rendering cleanly with no inlined secrets (T-06-01).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fhir-r4-model-emr-ingestion/02-RESEARCH.md
@.planning/phases/02-fhir-r4-model-emr-ingestion/02-PATTERNS.md

<interfaces>
From deploy/helm/veridoc/templates/postgres.yaml (analog for mongodb.yaml): `{{- if .Values.X.enabled }}` guard, `veridoc.componentName` helper, secretKeyRef for credentials, emptyDir-vs-PVC via persistence.enabled, readiness/liveness probes.
From deploy/helm/veridoc/templates/redis.yaml (analog for minio.yaml): single-port Deployment, command override, REDIS_PASSWORD secretKeyRef.
From deploy/helm/veridoc/templates/secrets.yaml + values.yaml secrets block: existing name/usernameKey/passwordKey convention for postgres/redis/keycloak.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MongoDB Helm template + values + secret ref</name>
  <read_first>
    - deploy/helm/veridoc/templates/postgres.yaml (full StatefulSet/Deployment + Service + secretKeyRef + probe pattern — analog from 02-PATTERNS.md)
    - deploy/helm/veridoc/values.yaml (existing section format + secrets block)
    - deploy/helm/veridoc/templates/secrets.yaml (existing secret-key declaration pattern)
  </read_first>
  <action>
    Create deploy/helm/veridoc/templates/mongodb.yaml cloning postgres.yaml: `{{- if
    .Values.mongodb.enabled }}` guard, component "mongodb", image mongo:7-jammy, kind
    Deployment (single-node, kind/CI scale), containerPort 27017, env
    MONGO_INITDB_ROOT_USERNAME + MONGO_INITDB_ROOT_PASSWORD via secretKeyRef referencing
    .Values.secrets.mongodb, emptyDir-vs-PVC via .Values.mongodb.persistence.enabled,
    readinessProbe exec `["mongosh","--eval","db.adminCommand('ping')"]`. Add the `mongodb:`
    section to values.yaml (enabled, image, replicas, port 27017, database veridoc_fhir,
    resources, persistence). Add the `mongodb:` entry to the values.yaml secrets block
    (name veridoc-mongodb, usernameKey MONGO_INITDB_ROOT_USERNAME, passwordKey
    MONGO_INITDB_ROOT_PASSWORD) and declare the corresponding key in secrets.yaml. Never
    inline credentials (T-06-01).
  </action>
  <verify>
    <automated>helm template deploy/helm/veridoc --set mongodb.enabled=true | grep -q 'mongo:7-jammy' &amp;&amp; helm template deploy/helm/veridoc | grep -A3 MONGO_INITDB_ROOT_PASSWORD | grep -q secretKeyRef &amp;&amp; ! helm template deploy/helm/veridoc | grep -iE 'password:\s*["'"'"']?[A-Za-z0-9]{6,}'</automated>
  </verify>
  <acceptance_criteria>
    - `helm template deploy/helm/veridoc` renders a mongodb Deployment with image mongo:7-jammy
    - MONGO_INITDB_ROOT_USERNAME and MONGO_INITDB_ROOT_PASSWORD both resolve via secretKeyRef (no literal password in rendered output)
    - values.yaml has a mongodb section (port 27017, database veridoc_fhir) and a secrets.mongodb entry
    - readinessProbe runs `mongosh --eval db.adminCommand('ping')`
  </acceptance_criteria>
  <done>MongoDB renders as a credential-referenced Deployment; no plaintext secrets in manifests.</done>
</task>

<task type="auto">
  <name>Task 2: MinIO Helm template + values + secret ref</name>
  <read_first>
    - deploy/helm/veridoc/templates/redis.yaml (single-port Deployment + command override + secretKeyRef pattern — analog from 02-PATTERNS.md)
    - deploy/helm/veridoc/values.yaml (after Task 1 edits — append the minio + ingestionService sections)
    - .planning/phases/02-fhir-r4-model-emr-ingestion/02-RESEARCH.md (§ Deployment Delta — minio values block)
  </read_first>
  <action>
    Create deploy/helm/veridoc/templates/minio.yaml cloning redis.yaml: `{{- if
    .Values.minio.enabled }}` guard, component "minio", image minio/minio (pinned
    RELEASE.* tag from values), command `["minio","server","/data","--console-address",":9001"]`,
    two containerPorts (9000 API + 9001 console) and two Service ports, env MINIO_ROOT_USER +
    MINIO_ROOT_PASSWORD via secretKeyRef referencing .Values.secrets.minio, emptyDir volume
    at /data. Add the `minio:` section to values.yaml (enabled, image with pinned tag,
    port 9000, consolePort 9001, defaultBucket veridoc-docs, resources, persistence) and the
    `ingestionService:` value section (image veridoc/ingestion-service:ci, port 8000,
    healthPath /healthz, config with keycloak issuer/jwks/audience=ingestion-service,
    redisUrl, blobEndpointUrl http://veridoc-minio:9000, blobBucket veridoc-docs) so 02-07 can
    consume it. Add the secrets.minio entry (name veridoc-minio, usernameKey MINIO_ROOT_USER,
    passwordKey MINIO_ROOT_PASSWORD) to values.yaml + secrets.yaml. The ingestion-service
    Deployment template itself is NOT created here (02-07 owns it after the image exists).
  </action>
  <verify>
    <automated>helm template deploy/helm/veridoc --set minio.enabled=true | grep -q 'minio/minio' &amp;&amp; helm template deploy/helm/veridoc | grep -A3 MINIO_ROOT_PASSWORD | grep -q secretKeyRef &amp;&amp; helm template deploy/helm/veridoc | grep -q 'console-address' &amp;&amp; grep -q 'veridoc-docs' deploy/helm/veridoc/values.yaml</automated>
  </verify>
  <acceptance_criteria>
    - `helm template deploy/helm/veridoc` renders a minio Deployment with two ports (9000, 9001)
    - MINIO_ROOT_USER/MINIO_ROOT_PASSWORD resolve via secretKeyRef (no literal secret in output)
    - values.yaml has minio (defaultBucket veridoc-docs) + ingestionService sections + secrets.minio entry
    - The minio image tag is a pinned RELEASE.* tag, not `latest`
  </acceptance_criteria>
  <done>MinIO renders as a credential-referenced Deployment; ingestionService values present for 02-07; no plaintext secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cluster ingress → MongoDB | clinical FHIR documents at rest |
| cluster ingress → MinIO | retained original scanned documents at rest |
| git → rendered manifests | secrets must never be committed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-INFRA-01 | Information Disclosure | MongoDB credentials | mitigate | MONGO_INITDB_ROOT_* sourced via secretKeyRef from veridoc-mongodb Secret; never inlined (T-06-01 precedent) |
| T-02-INFRA-02 | Information Disclosure | MinIO credentials | mitigate | MINIO_ROOT_* via secretKeyRef from veridoc-minio Secret; bucket veridoc-docs not public |
| T-02-INFRA-03 | Tampering | unpinned minio image tag | mitigate | Pin minio/minio to a RELEASE.* tag in values.yaml, not `latest` |
</threat_model>

<verification>
- `helm template deploy/helm/veridoc` renders without error with mongodb + minio enabled
- No literal password/secret string appears in rendered manifests (all via secretKeyRef)
</verification>

<success_criteria>
- MongoDB + MinIO land in the chart the same portable, secret-referenced way as Phase 1 Postgres/Redis
- ingestionService values block staged for 02-07; chart still renders cleanly
</success_criteria>

<output>
Create `.planning/phases/02-fhir-r4-model-emr-ingestion/02-02-SUMMARY.md` when done
</output>
