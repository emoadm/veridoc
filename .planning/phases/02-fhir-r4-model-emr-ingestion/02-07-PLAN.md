---
phase: 02-fhir-r4-model-emr-ingestion
plan: 07
type: execute
wave: 5
depends_on: ["02-02", "02-06"]
files_modified:
  - deploy/helm/veridoc/templates/ingestion-service.yaml
  - .github/workflows/ci.yml
autonomous: true
requirements: [EMR-01]
user_setup: []

must_haves:
  truths:
    - "The Helm chart renders an ingestion-service Deployment + Service AND a separate RQ-worker Deployment from the same image, with Mongo/Redis/blob env via secretKeyRef"
    - "CI installs tesseract-ocr, runs the veridoc-fhir + veridoc-ingestion + ingestion-service test suites with testcontainers (Mongo + MinIO), and they pass"
    - "The kind deploy job installs the full chart (incl. mongodb + minio + ingestion-service) and a smoke test POSTs to /ingest/{site_id} and confirms a FHIR resource appears in MongoDB"
  artifacts:
    - path: "deploy/helm/veridoc/templates/ingestion-service.yaml"
      provides: "ingestion-service Deployment + Service + RQ-worker Deployment"
      contains: "rq.serializers.JSONSerializer"
    - path: ".github/workflows/ci.yml"
      provides: "Phase 2 test + tesseract install + kind deploy + ingest smoke test"
      contains: "tesseract-ocr"
  key_links:
    - from: "deploy/helm/veridoc/templates/ingestion-service.yaml"
      to: "deploy/helm/veridoc/templates/secrets.yaml"
      via: "Mongo + MinIO credentials via secretKeyRef (T-06-01)"
      pattern: "secretKeyRef"
    - from: ".github/workflows/ci.yml"
      to: "POST /ingest/{site_id} → MongoDB"
      via: "kind deploy smoke test asserting a FHIR resource lands"
      pattern: "/ingest"
---

<objective>
Land the deploy + CI wiring that proves the phase end-to-end against fixtures: the
ingestion-service Deployment + a separate RQ-worker Deployment (same image, different command)
in the Helm chart, and the GitHub Actions delta that installs Tesseract, runs the Phase 2
testcontainer suites, deploys the full chart to an ephemeral kind cluster, and smoke-tests the
live ingest path (POST /ingest/{site_id} → FHIR resource in MongoDB). This is the same
provider-portable deploy path Phase 1 proved for the reference service.

Purpose: The four success criteria must be provable in CI; this plan closes the loop from
"libs + service exist" to "the deployed ingestion path works against fixtures".
Output: ingestion-service.yaml (API + worker Deployments) + ci.yml delta + the ingest smoke test.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-fhir-r4-model-emr-ingestion/02-RESEARCH.md
@.planning/phases/02-fhir-r4-model-emr-ingestion/02-PATTERNS.md
@.planning/phases/02-fhir-r4-model-emr-ingestion/02-02-SUMMARY.md
@.planning/phases/02-fhir-r4-model-emr-ingestion/02-06-SUMMARY.md

<interfaces>
From 02-02: values.yaml has ingestionService section (image veridoc/ingestion-service:ci, port 8000, healthPath /healthz, config keycloak/redis/blob), mongodb + minio sections + secrets.mongodb/secrets.minio entries.
From deploy/helm/veridoc/templates/reference-service.yaml (clone source): Deployment + Service + readiness/liveness probe + secretKeyRef env pattern.
From 02-06: ingestion-service image (uvicorn ingestion_service.main:app default CMD; tesseract installed); worker_main runnable as `rq worker ingestion --serializer rq.serializers.JSONSerializer`; tests at services/ingestion-service/tests/test_ingest_api.py.
From .github/workflows/ci.yml: existing deploy-kind job (lines 100-168) with a diagnose loop listing deployment names + a smoke-test step running the tamper-detection phase gate after helm install + kubectl wait.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ingestion-service + RQ-worker Helm Deployments</name>
  <read_first>
    - deploy/helm/veridoc/templates/reference-service.yaml (Deployment + Service + probe + secretKeyRef pattern, lines 1-107 — analog from 02-PATTERNS.md)
    - deploy/helm/veridoc/values.yaml (ingestionService + mongodb + minio + secrets sections from 02-02)
    - .planning/phases/02-fhir-r4-model-emr-ingestion/02-PATTERNS.md (ingestion-service.yaml section — env vars + worker Deployment command)
  </read_first>
  <action>
    Create deploy/helm/veridoc/templates/ingestion-service.yaml cloning reference-service.yaml:
    `{{- if .Values.ingestionService.enabled }}` guard, Deployment + Service for the API
    (image veridoc/ingestion-service, default CMD = uvicorn, port 8000, /healthz probes), plus
    a SECOND Deployment in the same file for the RQ worker — same image, command
    `["rq","worker","ingestion","--serializer","rq.serializers.JSONSerializer","--url","$(VERIDOC_REDIS_URL)"]`
    (no Service, no health port). Both Deployments get env: VERIDOC_MONGODB_URL (built from
    secret-referenced MONGO_USER/MONGO_PASSWORD + veridoc-mongodb:27017/veridoc_fhir),
    VERIDOC_REDIS_URL, VERIDOC_BLOB_ENDPOINT_URL, VERIDOC_BLOB_BUCKET, VERIDOC_BLOB_ACCESS_KEY +
    VERIDOC_BLOB_SECRET_KEY via secretKeyRef on .Values.secrets.minio, and the Keycloak issuer/
    jwks/audience from values config — never inline credentials (T-06-01).
  </action>
  <verify>
    <automated>helm template deploy/helm/veridoc | grep -c 'veridoc/ingestion-service' | grep -q '[2-9]' &amp;&amp; helm template deploy/helm/veridoc | grep -q 'rq.serializers.JSONSerializer' &amp;&amp; helm template deploy/helm/veridoc | grep -A4 VERIDOC_BLOB_SECRET_KEY | grep -q secretKeyRef &amp;&amp; helm lint deploy/helm/veridoc</automated>
  </verify>
  <acceptance_criteria>
    - `helm template deploy/helm/veridoc` renders two Deployments using the ingestion-service image (API + worker) and one Service for the API
    - the worker Deployment command uses rq.serializers.JSONSerializer (no pickle in cluster)
    - VERIDOC_BLOB_SECRET_KEY (and Mongo creds) resolve via secretKeyRef; no plaintext secret in rendered output
    - `helm lint deploy/helm/veridoc` passes
  </acceptance_criteria>
  <done>ingestion-service API + RQ worker render from the same image with secret-referenced Mongo/Redis/blob config; chart lints clean.</done>
</task>

<task type="auto">
  <name>Task 2: CI delta — Phase 2 tests, Tesseract, kind deploy + ingest smoke test</name>
  <read_first>
    - .github/workflows/ci.yml (lint/test job + deploy-kind job: diagnose loop lines 135-148, smoke-test step lines 149-166 — analog from 02-PATTERNS.md)
    - .planning/phases/02-fhir-r4-model-emr-ingestion/02-PATTERNS.md (§ CI Workflow Delta — deployment-name loop + smoke test additions)
    - services/ingestion-service/tests/test_ingest_api.py (the suite the CI smoke step exercises locally)
  </read_first>
  <action>
    Edit .github/workflows/ci.yml: (1) in the test job, add an apt step
    `sudo apt-get install -y tesseract-ocr tesseract-ocr-eng` (Pitfall 8) and ensure the suite
    runs libs/veridoc-fhir + libs/veridoc-ingestion + services/ingestion-service (testcontainers
    pull Mongo + MinIO — no extra services block needed); (2) in the deploy-kind job, build +
    kind-load the ingestion-service image, add veridoc-mongodb, veridoc-minio,
    veridoc-ingestion-service to the diagnose deployment-name loop, and add an ingest smoke step
    AFTER the existing tamper-detection gate: port-forward / curl POST /ingest/{site_id} with a
    Synthea native-FHIR fixture (authenticated via a minted test token), then assert a FHIR
    resource for that patient appears in MongoDB (mongosh query against the deployed Mongo).
    Keep the ephemeral-secrets / name-referenced pattern (no secret bytes in git, T-06-01).
  </action>
  <verify>
    <automated>grep -q 'tesseract-ocr' .github/workflows/ci.yml &amp;&amp; grep -q 'veridoc-ingestion-service' .github/workflows/ci.yml &amp;&amp; grep -q '/ingest' .github/workflows/ci.yml &amp;&amp; python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml')); print('yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - ci.yml is valid YAML and installs tesseract-ocr in the test job (Pitfall 8)
    - the test job runs the veridoc-fhir, veridoc-ingestion, and ingestion-service suites (testcontainers Mongo + MinIO)
    - the deploy-kind diagnose loop includes veridoc-mongodb, veridoc-minio, veridoc-ingestion-service
    - a smoke step POSTs to /ingest/{site_id} and asserts a FHIR resource lands in the deployed MongoDB (SC-1 end-to-end in CI)
    - no secret bytes added to git (T-06-01 preserved)
  </acceptance_criteria>
  <done>CI runs the Phase 2 suites with Tesseract + testcontainers and proves the deployed ingest path lands a FHIR resource in MongoDB against a fixture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| git → CI/deploy manifests | secrets must remain name-referenced + ephemeral |
| CI cluster → deployed ingest endpoint | smoke test exercises the live authenticated path |
| Redis → in-cluster RQ worker | job deserialization in the worker Deployment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-DEPLOY-01 | Information Disclosure | Mongo/MinIO creds in manifests | mitigate | all credentials via secretKeyRef from veridoc-mongodb / veridoc-minio Secrets; ephemeral name-referenced (T-06-01, Phase 1 precedent) |
| T-02-DEPLOY-02 | Tampering/Elevation | in-cluster RQ worker pickle | mitigate | worker Deployment command pins --serializer rq.serializers.JSONSerializer (no pickle in cluster) |
| T-02-DEPLOY-03 | Tampering | unverified deps in CI image | mitigate | image builds from committed uv.lock (PACKAGE-LEGITIMACY-approved 02-01); tesseract from distro apt |
| T-02-SC | Tampering | CI installs at deploy time | mitigate | lockfile-frozen install; T-02-SC carried from 02-01 |
</threat_model>

<verification>
- `helm lint deploy/helm/veridoc` passes; `helm template` renders API + worker Deployments with secret-referenced env
- ci.yml is valid YAML; installs tesseract; runs Phase 2 suites; kind deploy + ingest smoke step present
</verification>

<success_criteria>
- ingestion-service API + RQ worker deploy via the portable Helm chart (D-04 deploy delta)
- CI proves the deployed ingest path lands a FHIR resource in MongoDB against a fixture (SC-1 end-to-end)
- No pickle in the cluster; no secret bytes in git
</success_criteria>

<output>
Create `.planning/phases/02-fhir-r4-model-emr-ingestion/02-07-SUMMARY.md` when done
</output>
