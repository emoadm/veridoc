---
phase: 03-medidata-rave-mock-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/validation/PACKAGE-LEGITIMACY.md
  - libs/veridoc-rave/pyproject.toml
  - libs/veridoc-rave/README.md
  - libs/veridoc-rave/src/veridoc_rave/__init__.py
  - libs/veridoc-rave/src/veridoc_rave/port.py
  - libs/veridoc-rave/src/veridoc_rave/dtos.py
  - libs/veridoc-rave/tests/conftest.py
  - libs/veridoc-rave/tests/fixtures/subject_odm.xml
  - libs/veridoc-rave/tests/fixtures/write_response.xml
  - libs/veridoc-rave/tests/test_port_contract.py
  - pyproject.toml
  - uv.lock
autonomous: false
requirements: [RAVE-01]
user_setup: []

must_haves:
  truths:
    - "pydantic-xml and lxml carry an APPROVED verdict in PACKAGE-LEGITIMACY.md before any uv add runs"
    - "veridoc-rave resolves as a uv workspace member and imports cleanly"
    - "RavePort ABC cannot be instantiated directly (TypeError) and declares all 10 READ/WRITE methods"
    - "All 7 Rave DTOs exist as Pydantic v2 models with extra=forbid"
    - "subject_key in every DTO is documented and typed as the pseudonymized form (never the natural Rave SubjectKey)"
  artifacts:
    - path: "libs/veridoc-rave/src/veridoc_rave/port.py"
      provides: "RavePort ABC + RaveProfile frozen dataclass (D-01)"
      contains: "class RavePort(abc.ABC)"
    - path: "libs/veridoc-rave/src/veridoc_rave/dtos.py"
      provides: "Subject, CrfFieldValue, RaveAuditRecord, QueryStatus, ProtocolDeviation, Randomization, FreezeLockStatus, DiscrepancyNoteWrite DTOs (D-05/D-06)"
      contains: "class CrfFieldValue(BaseModel)"
    - path: "libs/veridoc-rave/tests/test_port_contract.py"
      provides: "ABC instantiation-rejection + swap-seam contract tests (SC-4)"
      exports: []
    - path: "docs/validation/PACKAGE-LEGITIMACY.md"
      provides: "Phase 03 package-legitimacy section with APPROVED verdicts for pydantic-xml + lxml"
      contains: "Phase 03"
  key_links:
    - from: "pyproject.toml"
      to: "libs/veridoc-rave"
      via: "[tool.uv.sources] veridoc-rave = { workspace = true }"
      pattern: "veridoc-rave = \\{ workspace = true \\}"
    - from: "libs/veridoc-rave/src/veridoc_rave/port.py"
      to: "libs/veridoc-rave/src/veridoc_rave/dtos.py"
      via: "type imports for method return signatures"
      pattern: "from veridoc_rave.dtos import"
---

<objective>
Establish the `veridoc-rave` library foundation: clear the supply-chain gate for the two
new packages (`pydantic-xml`, `lxml`), register the lib as a uv workspace member, and ship
the two stable contracts every downstream plan builds against — the `RavePort` ABC + frozen
`RaveProfile` config (D-01) and the typed Pydantic Rave DTOs (D-05/D-06). Also lay the
Wave-0 test scaffold for the lib so later tasks have a place to assert.

Purpose: This is the interface-first foundation. Plans 02 and 03 implement against these
contracts without exploring the codebase. The package gate is BLOCKING because both packages
are `[ASSUMED]` (slopcheck unavailable at research time) and DEC-supply-chain-gate forbids
any `uv add` before a human-approved PACKAGE-LEGITIMACY.md row.

Output: PACKAGE-LEGITIMACY.md Phase 03 section (APPROVED), `veridoc-rave` lib scaffold with
`port.py` + `dtos.py`, workspace registration, lib test scaffold (conftest, fixtures,
test_port_contract).
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-medidata-rave-mock-integration/03-CONTEXT.md
@.planning/phases/03-medidata-rave-mock-integration/03-RESEARCH.md
@.planning/phases/03-medidata-rave-mock-integration/03-PATTERNS.md
@.planning/phases/03-medidata-rave-mock-integration/03-VALIDATION.md

<interfaces>
<!-- Contracts the executor builds (port.py) and depends on (pseudonym). Use directly. -->

veridoc-pseudonym public API (libs/veridoc-pseudonym/src/veridoc_pseudonym/pseudonym.py):
  def patient_pseudonym(site_id: str, natural_id: str) -> str   # used by Plan 02 odm.py

SourceAdapter analog the Port mirrors (libs/veridoc-ingestion/src/veridoc_ingestion/adapter.py):
  - abc.ABC subclass + @dataclass(frozen=True) config dataclass + abstract method set + docstring discipline.

RaveProfile fields (D-01): base_url, username, password, study_oid, environment  (all str, frozen)
RavePort abstract methods (RESEARCH.md Pattern 1 / PATTERNS.md port.py):
  READ:  get_subjects, get_subject_data, get_query_status, get_protocol_deviations,
         get_randomization, get_freeze_lock_status
  WRITE: post_discrepancy_note, update_query_status, set_sdv_flag, flag_protocol_deviation

Rave DTOs (RESEARCH.md Pattern 4 / PATTERNS.md dtos.py) — all Pydantic v2 BaseModel, extra=forbid:
  RaveAuditRecord, CrfFieldValue, QueryStatus, Subject, ProtocolDeviation,
  Randomization, FreezeLockStatus, DiscrepancyNoteWrite
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy gate for pydantic-xml + lxml (BLOCKING, T-03-SC)</name>
  <files>docs/validation/PACKAGE-LEGITIMACY.md</files>
  <read_first>
    - docs/validation/PACKAGE-LEGITIMACY.md (existing format: Phase 01 + Phase 02 sections, table columns, sign-off block)
    - .planning/phases/03-medidata-rave-mock-integration/03-RESEARCH.md (§ Package Legitimacy Audit, lines 131-145; § Standard Stack New packages table)
  </read_first>
  <action>
    Append a `## Phase 03 (Medidata Rave Mock Integration)` section to docs/validation/PACKAGE-LEGITIMACY.md
    mirroring the Phase 02 section format (same table columns + threat-mapping note + review sign-off block),
    with one PyPI row each for pydantic-xml (assumed 2.21.0, repo github.com/dapper91/pydantic-xml,
    ~408K downloads/week) and lxml (assumed 6.1.1, repo github.com/lxml/lxml, de-facto standard), each with
    a blank Verified version + blank Verdict for the reviewer and a threat reference T-03-SC. Do NOT run any
    uv add — this is the gate; installs happen in Task 2 only after human approval. Then PAUSE for the
    human-verify checkpoint below. This is a blocking-human gate: it is never auto-approved (workflow.auto_advance
    is ignored for legitimacy checkpoints).
  </action>
  <what-built>
    Appended a `## Phase 03 (Medidata Rave Mock Integration)` section to
    docs/validation/PACKAGE-LEGITIMACY.md mirroring the Phase 02 section format, with one
    PyPI table row each for `pydantic-xml` (assumed 2.21.0, repo github.com/dapper91/pydantic-xml,
    ~408K downloads/week) and `lxml` (assumed 6.1.1, repo github.com/lxml/lxml, de-facto
    standard), each with a blank `Verified version` + blank `Verdict` for the reviewer, a
    threat-mapping note (T-03-SC supply-chain), and a Phase 03 review sign-off block. No
    `uv add` has run yet — installs are gated on this approval.
  </what-built>
  <how-to-verify>
    1. Open docs/validation/PACKAGE-LEGITIMACY.md and read the new Phase 03 section.
    2. For each package, visit the registry + source repo:
       - https://pypi.org/project/pydantic-xml/ and https://github.com/dapper91/pydantic-xml
       - https://pypi.org/project/lxml/ and https://github.com/lxml/lxml
       Confirm each is the authentic, maintained project (not a typo-squat), check
       maintainer, download counts, recent releases. Record latest-stable under
       `Verified version` and set `Verdict` to APPROVED or REJECTED.
    3. Reply "approved" once both verdicts are filled in, or name any package to reject
       (the executor will substitute the stdlib `xml.etree.ElementTree` fallback in Plan 02
       and remove the deps — RESEARCH.md § Fallback for ODM parsing).
  </how-to-verify>
  <verify>
    <automated>grep -v '^#' docs/validation/PACKAGE-LEGITIMACY.md | grep -c 'pydantic-xml'</automated>
  </verify>
  <resume-signal>Type "approved" (both verdicts APPROVED) or list packages to reject/substitute</resume-signal>
  <acceptance_criteria>
    - docs/validation/PACKAGE-LEGITIMACY.md contains a heading matching `Phase 03`
    - The Phase 03 table contains a row for `pydantic-xml` and a row for `lxml`
    - After human approval, both rows carry a non-blank `Verdict` (APPROVED or REJECTED) and a sign-off line
    - `grep -v '^#' docs/validation/PACKAGE-LEGITIMACY.md | grep -c 'pydantic-xml'` is >= 1
  </acceptance_criteria>
  <done>Phase 03 package-legitimacy section exists with APPROVED (or explicitly REJECTED) verdicts for both packages, human-approved. No uv add occurred before this.</done>
</task>

<task type="auto">
  <name>Task 2: veridoc-rave lib scaffold + workspace registration + uv add</name>
  <files>libs/veridoc-rave/pyproject.toml, libs/veridoc-rave/README.md, libs/veridoc-rave/src/veridoc_rave/__init__.py, pyproject.toml, uv.lock, libs/veridoc-rave/tests/conftest.py, libs/veridoc-rave/tests/fixtures/subject_odm.xml, libs/veridoc-rave/tests/fixtures/write_response.xml</files>
  <read_first>
    - libs/veridoc-ingestion/pyproject.toml (exact analog for the new pyproject)
    - libs/veridoc-ingestion/src/veridoc_ingestion/__init__.py (exact analog for __init__ exports)
    - pyproject.toml (root — [tool.uv.workspace] members glob "libs/*"; [tool.uv.sources] block lines 20-27)
    - .planning/phases/03-medidata-rave-mock-integration/03-PATTERNS.md (veridoc-rave/pyproject.toml pattern lines 60-96; conftest pattern; CDISC ODM fixture shape from RESEARCH.md Pattern 3)
    - .planning/phases/03-medidata-rave-mock-integration/03-RESEARCH.md (Pattern 3 ODM READ + WRITE response XML for fixture content)
  </read_first>
  <action>
    Create libs/veridoc-rave/pyproject.toml per PATTERNS.md (name veridoc-rave, requires-python >=3.12,
    dependencies: pydantic>=2.0, httpx>=0.27, pydantic-xml>=2.21.0, lxml>=6.1.1, veridoc-pseudonym;
    hatchling build, packages = ["src/veridoc_rave"]). Add `veridoc-rave = { workspace = true }` to the
    root pyproject.toml [tool.uv.sources] block (members glob already covers libs/*). Run
    `uv add pydantic-xml lxml --package veridoc-rave` ONLY AFTER Task 1 approval — this updates uv.lock.
    Create __init__.py exporting RavePort, RaveProfile, MdrwsHttpAdapter, and all DTO names (the
    MdrwsHttpAdapter import will be wired in Plan 02 — for now export the Port + DTOs that exist after
    Task 3; use a lazy/guarded import or leave the adapter export to Plan 02's __init__ edit). Create a
    minimal README.md describing the lib per D-11. Create the lib test scaffold: tests/conftest.py
    (Wave-0 — for now the mock TestClient fixture is provided by Plan 03; here add only the
    `_fresh_keystore`-style env setup needed for pseudonym, and a placeholder for the mock fixture),
    tests/fixtures/subject_odm.xml (a CDISC ODM 1.3 Snapshot with ClinicalData/SubjectData/ItemData +
    AuditRecord + an mdsol:Query child, per RESEARCH.md Pattern 3 READ structure — used by Plan 02
    test_odm), and tests/fixtures/write_response.xml (an RWSPostResponse success element per RESEARCH.md
    Pattern 2). Reuse Phase 2 idioms verbatim where the analog holds (PATTERNS.md says ingestion is a
    near-exact structural clone). If Task 1 REJECTED the packages, omit pydantic-xml + lxml from
    dependencies (Plan 02 uses stdlib ElementTree) and skip the uv add.
  </action>
  <verify>
    <automated>uv sync 2>&1 | tail -5 && uv run python -c "import veridoc_rave" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `uv run python -c "import veridoc_rave"` exits 0
    - `grep -c 'veridoc-rave = { workspace = true }' pyproject.toml` is 1
    - libs/veridoc-rave/tests/fixtures/subject_odm.xml contains `ClinicalData` and `mdsol:Query`
    - libs/veridoc-rave/tests/fixtures/write_response.xml contains `IsTransactionSuccessful`
    - If packages APPROVED: `uv pip show pydantic-xml` and `uv pip show lxml` both succeed; uv.lock changed
  </acceptance_criteria>
  <done>veridoc-rave is a resolvable workspace member that imports cleanly; the two new packages are installed (or omitted if rejected); ODM fixtures exist for Plan 02 tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RavePort ABC + RaveProfile + Rave DTOs (the swap-seam contracts)</name>
  <files>libs/veridoc-rave/src/veridoc_rave/port.py, libs/veridoc-rave/src/veridoc_rave/dtos.py, libs/veridoc-rave/tests/test_port_contract.py</files>
  <behavior>
    - Test: RavePort() raises TypeError (abstract — cannot instantiate; SC-4 swap-seam guarantee that all impls share one interface).
    - Test: RavePort declares exactly the 10 abstract methods (6 READ + 4 WRITE); a subclass missing any one cannot instantiate.
    - Test: RaveProfile is frozen — assigning to .base_url after construction raises FrozenInstanceError.
    - Test: each DTO rejects an unknown extra field (extra="forbid" raises ValidationError).
    - Test: CrfFieldValue carries audit_records: list[RaveAuditRecord] and a subject_key field documented as pseudonymized.
  </behavior>
  <read_first>
    - libs/veridoc-ingestion/src/veridoc_ingestion/adapter.py (SourceAdapter ABC + SourceProfile frozen dataclass + docstring discipline — exact idiom to mirror)
    - libs/veridoc-audit/src/veridoc_audit/models.py (AuditEvent ConfigDict + extra="forbid" DTO discipline, lines 40-83)
    - .planning/phases/03-medidata-rave-mock-integration/03-PATTERNS.md (port.py pattern lines 100-194; dtos.py pattern lines 198-222)
    - .planning/phases/03-medidata-rave-mock-integration/03-RESEARCH.md (Pattern 1 RavePort signatures; Pattern 4 full DTO field sets)
    - libs/veridoc-ingestion/tests/test_adapters.py (role-match analog for ABC contract test style)
  </read_first>
  <action>
    Create port.py with a frozen `RaveProfile` dataclass (fields: base_url, username, password,
    study_oid, environment — all str) and the `RavePort(abc.ABC)` class declaring the 10 abstract
    methods from RESEARCH.md Pattern 1: READ — get_subjects, get_subject_data, get_query_status,
    get_protocol_deviations, get_randomization, get_freeze_lock_status; WRITE — post_discrepancy_note,
    update_query_status, set_sdv_flag, flag_protocol_deviation. Mirror the SourceAdapter docstring
    discipline; document that the Port is the abstraction layer (D-01) and ODM-XML / HTTP Basic / URL
    conventions never cross it. Create dtos.py with the 8 Pydantic v2 models per RESEARCH.md Pattern 4
    (RaveAuditRecord, CrfFieldValue, QueryStatus, Subject, ProtocolDeviation, Randomization,
    FreezeLockStatus, DiscrepancyNoteWrite), each with model_config = ConfigDict(extra="forbid"), and a
    comment on every subject_key field stating it is the pseudonymized form (D-07) — never the natural
    Rave SubjectKey. port.py imports the DTO names for its return-type annotations. Write
    test_port_contract.py asserting the behaviors above (ABC non-instantiation, missing-method rejection,
    frozen profile, extra-field rejection) — these prove SC-4 (one interface = config-only swap).
  </action>
  <verify>
    <automated>uv run pytest libs/veridoc-rave/tests/test_port_contract.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `uv run pytest libs/veridoc-rave/tests/test_port_contract.py -q` exits 0
    - `grep -c 'class RavePort(abc.ABC)' libs/veridoc-rave/src/veridoc_rave/port.py` is 1
    - `grep -c '@abc.abstractmethod' libs/veridoc-rave/src/veridoc_rave/port.py` is 10
    - `grep -c 'extra="forbid"' libs/veridoc-rave/src/veridoc_rave/dtos.py` is >= 7
    - `uv run python -c "from veridoc_rave.port import RavePort; RavePort()"` raises TypeError (abstract)
  </acceptance_criteria>
  <done>RavePort + RaveProfile + all 8 DTOs exist, the ABC is non-instantiable with 10 abstract methods, and test_port_contract.py is green — the swap-seam contract (SC-4) is proven at the unit level.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → package registry | new `uv add` pulls untrusted third-party code into the build |
| adapter → agent code (future) | DTOs are the only objects allowed to cross; natural SubjectKey must not |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | `uv add pydantic-xml lxml` (supply chain) | mitigate | Blocking human-verify checkpoint (Task 1) gates the install behind a PACKAGE-LEGITIMACY.md APPROVED verdict per DEC-supply-chain-gate. `[ASSUMED]` packages are never auto-approved. |
| T-03-01 | Information Disclosure | Rave Subject ID in DTOs | mitigate | dtos.py documents and types subject_key as the pseudonymized form only; the natural SubjectKey is pseudonymized at the Plan 02 adapter boundary (D-07) and never reaches a DTO field. |
| T-03-02 | Tampering | DTO field hygiene | mitigate | Every DTO uses `extra="forbid"`, so malformed ODM parsing (Plan 02) fails fast with ValidationError rather than silently carrying unexpected fields. |
</threat_model>

<verification>
- `uv run python -c "import veridoc_rave"` succeeds (workspace resolves).
- `uv run pytest libs/veridoc-rave/tests/test_port_contract.py -q` green.
- PACKAGE-LEGITIMACY.md Phase 03 section has filled-in verdicts.
- No `uv add` ran before the Task 1 approval (audit by checking git diff ordering / the section exists before pyproject deps).
</verification>

<success_criteria>
- Supply-chain gate cleared (or fallback selected) — deliberate, human-approved.
- veridoc-rave imports cleanly as a workspace member.
- RavePort ABC + RaveProfile + 8 DTOs exist and the swap-seam contract test (SC-4) is green.
- ODM fixtures staged for Plan 02 ODM-parsing tests.
</success_criteria>

<output>
Create `.planning/phases/03-medidata-rave-mock-integration/03-01-SUMMARY.md` when done.
</output>
