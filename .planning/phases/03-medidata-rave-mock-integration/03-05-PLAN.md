---
phase: 03-medidata-rave-mock-integration
plan: 05
type: execute
wave: 4
depends_on: ["03-02", "03-03", "03-04"]
files_modified:
  - deploy/helm/veridoc/templates/rave-integration.yaml
  - deploy/helm/veridoc/templates/rave-mock.yaml
  - deploy/helm/veridoc/values.yaml
  - Taskfile.yml
  - .github/workflows/ci.yml
autonomous: true
requirements: [RAVE-01]
user_setup: []

must_haves:
  truths:
    - "rave-integration (API + RQ worker) and rave-mock deploy to the kind cluster via Helm"
    - "The webhook HMAC secret is created ephemerally in deploy:kind and injected via secretKeyRef (never committed)"
    - "CI runs the Phase 3 unit/integration suites and a kind smoke test exercising READ/WRITE/WEBHOOK/SWAP end-to-end"
    - "helm lint is clean with the two new templates enabled"
    - "The kind smoke test proves all 4 Success Criteria against the deployed mock"
  artifacts:
    - path: "deploy/helm/veridoc/templates/rave-integration.yaml"
      provides: "API + RQ-worker Deployments + Service (D-09/D-11), secretKeyRef webhook secret"
      contains: "raveIntegration.enabled"
    - path: "deploy/helm/veridoc/templates/rave-mock.yaml"
      provides: "Stateful mock Deployment + Service on 8001 (D-02/D-11)"
      contains: "raveMock.enabled"
    - path: ".github/workflows/ci.yml"
      provides: "Phase 3 test suites + Rave kind smoke test (SC-1..SC-4)"
      contains: "Rave smoke test"
  key_links:
    - from: "deploy/helm/veridoc/templates/rave-integration.yaml"
      to: "deploy/helm/veridoc/values.yaml"
      via: "secretKeyRef VERIDOC_RAVE_WEBHOOK_SECRET from secrets.rave (T-06-01)"
      pattern: "secretKeyRef"
    - from: "Taskfile.yml"
      to: "deploy/helm/veridoc"
      via: "create veridoc-rave secret + helm upgrade --set rave image overrides + kubectl wait"
      pattern: "veridoc-rave"
    - from: ".github/workflows/ci.yml"
      to: "libs/veridoc-rave/tests/test_adapter_read.py"
      via: "kind smoke test runs READ/WRITE/WEBHOOK/SWAP against deployed services"
      pattern: "rave-mock"
---

<objective>
Deploy the two new services and prove the phase end-to-end: Helm templates for
`rave-integration` (API + RQ worker, secretKeyRef webhook secret) and `rave-mock` (stateful
emulator on 8001), `values.yaml` stanzas + secret ref, `Taskfile` `deploy:kind` extensions
(ephemeral webhook secret, image overrides, deployment waits), and a CI `Rave smoke test`
that exercises READ/WRITE/WEBHOOK/SWAP against the deployed mock — the single proof of all
four RAVE-01 Success Criteria. Mirror Phase 2's deploy+CI pattern exactly (D-11).

Purpose: SC-1..SC-4 are only fully proven against the DEPLOYED mock over real HTTP in a kind
cluster (VALIDATION.md manual-only row). This plan turns the per-task unit/integration green
into the milestone-grade end-to-end proof in CI, with no plaintext secrets (T-06-01).

Output: rave-integration.yaml, rave-mock.yaml, values.yaml additions, Taskfile + ci.yml deltas.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-medidata-rave-mock-integration/03-RESEARCH.md
@.planning/phases/03-medidata-rave-mock-integration/03-PATTERNS.md
@.planning/phases/03-medidata-rave-mock-integration/03-VALIDATION.md
@.planning/phases/03-medidata-rave-mock-integration/03-02-SUMMARY.md
@.planning/phases/03-medidata-rave-mock-integration/03-03-SUMMARY.md
@.planning/phases/03-medidata-rave-mock-integration/03-04-SUMMARY.md

<interfaces>
<!-- Deploy/CI analogs. Mirror these exactly (D-11). -->

Helm analog: deploy/helm/veridoc/templates/ingestion-service.yaml (API + worker two-Deployment
  pattern, secretKeyRef env injection lines 66-114, /healthz probes). Template guard idiom
  `{{- if .Values.ingestionService.enabled }}`.

values.yaml: add raveIntegration + raveMock stanzas (PATTERNS.md lines 853-893) and a
  secrets.rave entry {name: veridoc-rave, webhookSecretKey: VERIDOC_RAVE_WEBHOOK_SECRET}.

Taskfile.yml deploy:kind (lines 62-148): add ephemeral `kubectl create secret generic veridoc-rave`
  with `openssl rand -hex 32`; add RAVE_INTEGRATION_IMAGE + RAVE_MOCK_IMAGE vars + image build+load;
  add --set image overrides to helm upgrade; add kubectl wait for the 3 new deployments.

ci.yml: integration job adds Phase 3 pytest suites; deploy-kind job adds the Rave smoke test step
  (PATTERNS.md lines 945-992; VALIDATION.md kind smoke test SC-1..SC-4).

Service ports: rave-integration 8000, rave-mock 8001. Mock URL in cluster:
  http://veridoc-rave-mock:8001/RaveWebServices
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Helm templates + values for rave-integration + rave-mock</name>
  <files>deploy/helm/veridoc/templates/rave-integration.yaml, deploy/helm/veridoc/templates/rave-mock.yaml, deploy/helm/veridoc/values.yaml</files>
  <read_first>
    - deploy/helm/veridoc/templates/ingestion-service.yaml (two-Deployment API+worker analog; secretKeyRef lines 66-114; probes)
    - deploy/helm/veridoc/values.yaml (existing ingestionService stanza + secrets stanza to extend)
    - deploy/helm/veridoc/templates/_helpers.tpl (name/label helpers)
    - .planning/phases/03-medidata-rave-mock-integration/03-PATTERNS.md (rave-integration.yaml lines 812-831; rave-mock.yaml 833-847; values.yaml 850-900; K8s Secret pattern 1150-1162)
  </read_first>
  <action>
    Create rave-integration.yaml mirroring ingestion-service.yaml under a raveIntegration.enabled guard:
    an API Deployment (CMD uvicorn rave_integration.main:app, port 8000) + a ClusterIP Service + a worker
    Deployment (CMD rq worker rave-events --serializer rq.serializers.JSONSerializer --url the redis url),
    same image. Keep the Postgres (audit) + Redis env blocks verbatim; REMOVE MongoDB/MinIO/blob env vars.
    Add env vars: VERIDOC_RAVE_WEBHOOK_SECRET via secretKeyRef from secrets.rave (T-06-01 — never inlined),
    and VERIDOC_RAVE_BASE_URL as a non-secret value pointing at http://veridoc-rave-mock:8001/RaveWebServices.
    Keep readiness/liveness probes on /healthz. Create rave-mock.yaml under a raveMock.enabled guard: a
    single Deployment (uvicorn rave_mock.main:app port 8001) + ClusterIP Service; no Postgres/Redis/KMS/
    secret refs (mock is self-contained, no audit writes); /healthz probes. Extend values.yaml with the
    raveIntegration + raveMock stanzas and the secrets.rave entry per PATTERNS.md. Run helm lint and fix
    until clean.
  </action>
  <verify>
    <automated>helm lint deploy/helm/veridoc 2>&1 | tail -5 && helm template deploy/helm/veridoc 2>&1 | grep -Ec 'veridoc-rave-integration|veridoc-rave-mock'</automated>
  </verify>
  <acceptance_criteria>
    - `helm lint deploy/helm/veridoc` reports 0 failures
    - `helm template deploy/helm/veridoc` renders Deployments named veridoc-rave-integration, veridoc-rave-integration-worker, and veridoc-rave-mock
    - `grep -c 'secretKeyRef' deploy/helm/veridoc/templates/rave-integration.yaml` is >= 1
    - `grep -Ec 'mongo|minio|blob' deploy/helm/veridoc/templates/rave-integration.yaml` is 0
    - `grep -c '8001' deploy/helm/veridoc/templates/rave-mock.yaml` is >= 1
    - `grep -c 'rave:' deploy/helm/veridoc/values.yaml` is >= 1 (secrets.rave entry)
  </acceptance_criteria>
  <done>Both templates render and helm lint is clean; rave-integration injects the webhook secret via secretKeyRef (no plaintext); rave-mock runs standalone on 8001; values carry both stanzas + the secret ref.</done>
</task>

<task type="auto">
  <name>Task 2: Taskfile deploy:kind + CI Phase 3 suites + Rave kind smoke test (SC-1..SC-4)</name>
  <files>Taskfile.yml, .github/workflows/ci.yml</files>
  <read_first>
    - Taskfile.yml (deploy:kind task lines 62-148: secret creation, image build+load, helm upgrade --set, kubectl wait; ingestion image var pattern)
    - .github/workflows/ci.yml (integration job ~line 108; deploy-kind job lines 110-215 incl. ingestion smoke test + Diagnose-failure loop)
    - .planning/phases/03-medidata-rave-mock-integration/03-PATTERNS.md (Taskfile lines 904-937; ci.yml lines 941-992)
    - .planning/phases/03-medidata-rave-mock-integration/03-VALIDATION.md (Kind Smoke Test SC-1..SC-4 sequence lines 940-947)
  </read_first>
  <action>
    Extend Taskfile.yml deploy:kind: after the MinIO secret block add an ephemeral webhook secret using
    kubectl create secret generic veridoc-rave with VERIDOC_RAVE_WEBHOOK_SECRET set to the output of
    openssl rand -hex 32, applied via --dry-run=client -o yaml piped to kubectl apply (throwaway, never
    committed — T-06-01). Add RAVE_INTEGRATION_IMAGE + RAVE_MOCK_IMAGE vars (default veridoc/rave-integration:ci,
    veridoc/rave-mock:ci) and the corresponding docker build + kind load steps mirroring the ingestion
    image step. Add the two --set rave image repository/tag overrides to the helm upgrade --install. Add
    kubectl wait --for=condition=available for deployments veridoc-rave-integration,
    veridoc-rave-integration-worker, veridoc-rave-mock. Extend ci.yml: in the integration job add a
    pytest step for libs/veridoc-rave/tests/ and one for services/rave-integration/tests/. In the
    deploy-kind job, after the ingestion smoke test, add a "Rave smoke test — READ/WRITE/WEBHOOK/SWAP
    end-to-end (SC-1..SC-4)" step per PATTERNS.md: port-forward rave-mock:8001 and rave-integration:8000,
    read the webhook secret + PG password from K8s Secrets, wait for both forwards, then run the adapter
    READ/WRITE tests + the webhook test + the port_contract test against the live services (env:
    VERIDOC_RAVE_MOCK_URL, VERIDOC_RAVE_INTEGRATION_URL, VERIDOC_RAVE_WEBHOOK_SECRET,
    VERIDOC_TEST_DATABASE_URL). The smoke test proves: SC-1 read typed CRF fields; SC-2 write a
    discrepancy note then read back status Open; SC-3 trigger a mock webhook and assert the
    rave:webhook:dispatched audit row in Postgres; SC-4 instantiate the same MdrwsHttpAdapter against two
    base_urls confirming config-only swap. Add the three new deployments to the deploy-kind
    Diagnose-failure loop.
  </action>
  <verify>
    <automated>python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('ci.yml valid')" && grep -c 'Rave smoke test' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0 (valid YAML)
    - `grep -c 'Rave smoke test' .github/workflows/ci.yml` is >= 1
    - `grep -c 'veridoc-rave' Taskfile.yml` is >= 1 (ephemeral secret creation)
    - `grep -c 'openssl rand' Taskfile.yml` is >= 1
    - `grep -Ec 'veridoc-rave-integration-worker|veridoc-rave-mock' Taskfile.yml` is >= 1 (kubectl wait)
    - `grep -c 'libs/veridoc-rave/tests' .github/workflows/ci.yml` is >= 1 (integration suite step)
  </acceptance_criteria>
  <done>deploy:kind builds+loads+deploys both new services with an ephemeral webhook secret and waits on all 3 deployments; CI runs Phase 3 suites and a Rave kind smoke test proving SC-1..SC-4 end-to-end; ci.yml is valid YAML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Helm values/templates → cluster | secrets must arrive via secretKeyRef, never inlined |
| CI runner → kind cluster | port-forwarded service access; secrets read from K8s at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-15 | Information Disclosure | Webhook secret in Helm/values/git | mitigate | VERIDOC_RAVE_WEBHOOK_SECRET arrives only via secretKeyRef; deploy:kind generates it ephemerally with openssl rand and applies it as a K8s Secret — never committed (T-06-01). |
| T-03-16 | Tampering | Untagged/`latest` images | mitigate | rave images are tagged (:ci default, overridable); helm template renders explicit repository/tag — no `latest`. |
| T-03-17 | Spoofing | Smoke test bypassing real HTTP | mitigate | The smoke test port-forwards the DEPLOYED mock + service and drives the real MdrwsHttpAdapter over HTTP (D-01) — it does not use in-process fakes, so SC-1..SC-4 reflect the production path. |
| T-03-SC | Tampering | (carried) supply-chain for new packages | mitigate | Already gated in Plan 01 (PACKAGE-LEGITIMACY.md APPROVED); lockfile pins the approved versions used in the images. |
</threat_model>

<verification>
- `helm lint deploy/helm/veridoc` clean; helm template renders all 3 new deployments.
- ci.yml is valid YAML and contains the Rave smoke test step.
- Taskfile deploy:kind creates the ephemeral secret, builds+loads both images, waits on all 3 deployments.
- (CI) Rave smoke test proves SC-1 READ, SC-2 WRITE+read-back, SC-3 webhook→dispatched audit, SC-4 config-only swap.
</verification>

<success_criteria>
- Both services deploy to kind via Helm with no plaintext secrets (T-06-01).
- CI runs the Phase 3 suites and the end-to-end kind smoke test.
- The smoke test is the single proof of all four RAVE-01 Success Criteria against the deployed mock.
</success_criteria>

<output>
Create `.planning/phases/03-medidata-rave-mock-integration/03-05-SUMMARY.md` when done.
</output>
