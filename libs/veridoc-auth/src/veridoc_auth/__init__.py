"""veridoc_auth — OIDC authn + 8-role RBAC middleware (skeleton).

Implemented in plan 01-04: Keycloak JWKS signature verification, iss/aud/exp + MFA
acr enforcement, 8-role RBAC, and IP-allowlist hooks (D-01/D-02).
"""
