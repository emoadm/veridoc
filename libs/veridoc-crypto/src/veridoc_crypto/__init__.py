"""veridoc_crypto — app-level envelope-encryption helper (skeleton).

Implemented in plan 01-03: AES-256-GCM envelope encryption behind a portable KMS
abstraction, with a master-key + per-patient HKDF-derived key hierarchy (D-11).
"""
