"""veridoc_pseudonym — deterministic per-patient pseudonymization (skeleton).

Implemented in plan 01-03: HMAC(patient_key, natural_id) deterministic tokens with
crypto-shredding erasure via per-patient key deletion (D-12).
"""
