"""veridoc_tenancy — request-scoped tenancy context middleware (skeleton).

Implemented in plan 01-04: site/study claims extracted into a request-scoped context
(contextvars), fail-closed query scoping + audit stamping (D-03).
"""
